# UI

Host-eigene ImGui-Fenster und die intern implementierte Script-UI-Fassade.

Host-Fenster werden nach Feature gruppiert. Script-Bindings exponieren nur die versionierte `ui`-API und niemals ImGui-Interna.

